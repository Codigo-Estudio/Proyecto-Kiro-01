# Tech Stack

## Build System & Dependencies

This is a starter kit project with no build system configured yet. The project includes a `.gitignore` that shows readiness for Node.js and Python tooling (has sections for node_modules, npm-debug.log, Python cache, etc.), but no actual dependencies or build configuration is present in the repository.

## Tech Stack

- **Runtime**: Not yet determined (project structure suggests potential for JavaScript/TypeScript or Python)
- **Game Framework**: Not yet determined
- **Assets**: PNG sprites (ghosty.png), WAV audio files (jump.wav, game_over.wav)

## Common Commands

None configured yet. When build tools are added, they should be documented here.

## Testing

No testing framework configured yet.